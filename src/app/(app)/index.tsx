import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function DashboardScreen() {
  return (
    <PlaceholderScreen
      title="Dashboard"
      phaseNote="Portfolio summary cards, filters, and quick links arrive in Phase 9 (Dashboard & Analytics), once properties and inspections exist to summarize."
    />
  );
}
