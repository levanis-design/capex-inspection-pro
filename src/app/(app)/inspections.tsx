import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function InspectionsScreen() {
  return (
    <PlaceholderScreen
      title="Inspections"
      phaseNote="The location-driven field inspection workflow arrives in Phase 5, with offline support in Phase 6."
    />
  );
}
